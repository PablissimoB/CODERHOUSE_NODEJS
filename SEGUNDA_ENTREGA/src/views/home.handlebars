<h1>Lista de productos</h1>
<hr>
{{#each products}}
    <div class="card text-bg-success mb-3" style="width: 18rem;">
        <div class="card-body">
            <h5 class="card-title">{{this.title}}</h5>
            <p class="card-text">{{this.description}}</p>
            <p class="card-text">precio : {{this.price}} - stock: {{this.stock}}</p>
            <a href="/Product/{{this._id}}"><button class="btn btn-primary"> View</button></a></li>
            <button onclick="addToCart('64fd09d885e276a633a2e8aa','{{this._id}}');" class="btn btn-secondary"> Add</button>
        </div>
    </div>

{{/each}}